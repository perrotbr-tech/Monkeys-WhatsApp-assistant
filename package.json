{
  "name": "monkeys-whatsapp-assistant",
  "version": "0.1.0",
  "description": "A WhatsApp assistant built on the WhatsApp Cloud API webhook model.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
