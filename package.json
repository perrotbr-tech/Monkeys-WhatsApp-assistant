{
  "name": "monkeys-whatsapp-assistant",
  "version": "0.1.0",
  "description": "Hybrid gym assistant: deterministic operations + local NLU intent layer (no external LLM).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "demo": "node scripts/demo-conversation.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
